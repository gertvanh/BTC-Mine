#pragma once

// Public GitHub repo used for OTA assets (must be public for pull-OTA without a token).
// Set your username/org, then create the repo and push tags to publish releases.
#define OTA_GITHUB_OWNER "gertvanh"
#define OTA_GITHUB_REPO "BTC-Mine"
